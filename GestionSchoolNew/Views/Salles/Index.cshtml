@model IEnumerable<GestionSchoolNew.Models.Salle>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleSalle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleSalle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSalle })
        </td>
    </tr>
}

</table>
