@model IEnumerable<GestionSchoolNew.Models.Jour>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleJour)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleJour)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdJour }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdJour }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdJour })
        </td>
    </tr>
}

</table>
