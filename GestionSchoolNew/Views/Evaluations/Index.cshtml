@model IEnumerable<GestionSchoolNew.Models.Evaluation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleEvaluation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleEvaluation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEvaluation }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEvaluation }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEvaluation })
        </td>
    </tr>
}

</table>
