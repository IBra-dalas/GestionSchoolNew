@model IEnumerable<GestionSchoolNew.Models.ModePayement>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleModePayement)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleModePayement)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdModePayement }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdModePayement }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdModePayement })
        </td>
    </tr>
}

</table>
