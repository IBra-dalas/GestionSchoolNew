@model IEnumerable<GestionSchoolNew.Models.Absence>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MotifAbsence)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateAbsence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MotifAbsence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateAbsence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAbsence }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAbsence }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAbsence })
        </td>
    </tr>
}

</table>
