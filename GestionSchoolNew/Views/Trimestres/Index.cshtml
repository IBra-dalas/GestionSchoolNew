@model IEnumerable<GestionSchoolNew.Models.Trimestre>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleTrimestre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DebutTrimestre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinTrimestre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleTrimestre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DebutTrimestre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinTrimestre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTrimestre }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTrimestre }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTrimestre })
        </td>
    </tr>
}

</table>
