@model IEnumerable<GestionSchoolNew.Models.Classe>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleClasse)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleClasse)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdClasse }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdClasse }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdClasse })
        </td>
    </tr>
}

</table>
