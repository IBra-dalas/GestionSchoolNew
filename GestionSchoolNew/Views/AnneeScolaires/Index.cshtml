@model IEnumerable<GestionSchoolNew.Models.AnneeScolaire>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleAnneeScolaire)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleAnneeScolaire)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAnneeScolaire }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAnneeScolaire }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAnneeScolaire })
        </td>
    </tr>
}

</table>
